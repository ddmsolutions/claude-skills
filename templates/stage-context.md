# Stage: [name]

> Layer 2 contract. 200-500 tokens. The Inputs table is the control point: it decides
> what loads, so the model gets signal, not the whole workspace.

## Purpose
[One sentence. A stage that researches does not also draft.]

## Inputs
- Layer 4 (working, transform this): ../[previous-stage]/output/[file-pattern]
- Layer 3 (reference, obey this): ../../_config/[constraints.md]
- Layer 3 (reference, obey this): references/[stage-specific file]

## Process
1. [Read X, produce Y. Compilation framing, not "explore X". Max 5 steps.]
2. [step]
3. [step]

## Output
- Format: [exact format, length or structural constraint]
- Written to: output/[filename-pattern]
- Must include: [non-negotiables]
- Must NOT include: [explicit exclusions: no unsolicited summaries, no scope creep]
- Done looks like: [one sentence a reviewer could check]

## Verify
- [Which earlier stage output must this stay consistent with, and the check to run.]

## Routing
- Next: ../[next-stage]/ after human review of output/
- On failure: [what to do; usually fix the source that caused it, not the output]

Last updated: [date]
